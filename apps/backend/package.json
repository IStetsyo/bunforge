{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-types": "tsc -p tsconfig.router.json",
    "watch-types": "tsc -p tsconfig.router.json --watch --preserveWatchOutput",
    "prebuild": "bun run generate-types",
    "dev": "bun run watch-types & bun run --watch src/index.ts",
    "build": "bun run generate-types && bun build --compile --target bun --outfile build/server src/index.ts"
  },
  "dependencies": {
    "@orpc/server": "^1.13.5",
    "elysia": "latest",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
